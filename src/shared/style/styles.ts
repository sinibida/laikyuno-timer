import { Interpolation } from "@emotion/react";
import { Theme } from "@mui/material";
import buttonStyles from "./components/Button";

const styles: Interpolation<Theme> = [buttonStyles];

export default styles;
